import React from 'react';
import { motion } from 'framer-motion';
import { resumeData } from '../data/resume.jsx';

const Skills = () => {
  return null; // Skills are now integrated into the About section for a cleaner layout
};

export default Skills;